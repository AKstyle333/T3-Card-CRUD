import React from "react";

const Card = (props) => {
    // const [cardData, setCardData] = useState({props.data});
    // console.log(props.data[0].name, "props");

    return (
        <>
           
        </>
    );
};

export default Card;
